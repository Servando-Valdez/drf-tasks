/**
 * `FILTERS` is an object that defines constants for filtering tasks.
 * It provides a set of filter options for displaying tasks based on their status.
 */
export const FILTERS = {
    ALL: 'all',
    COMPLETED: 'completed',
    PENDING: 'pending'
}